version: "3.8"

services:
  web:
    build: .
    container_name: new-world-guide
    ports:
      - "8080:8080"
    volumes:
      - .:/var/www/html
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
    restart: unless-stopped
